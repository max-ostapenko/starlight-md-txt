---
"starlight-md-txt": minor
---

Add support for custom file format extensions (`.md` by default, `.md.txt` optionally), dynamic response headers (`text/markdown` or `text/plain`), and automatic raw Markdown fallback 404 page generation.
